CREATE TABLE salario_classe_oficial(
id INTEGER PRIMARY KEY AUTOINCREMENT,
nivel TEXT,
salario varchar(50));

CREATE TABLE usuarios_mobills(
id INTEGER PRIMARY KEY AUTOINCREMENT,
DataCadastro DATE,
DataCompra DATE,
status TEXTE);